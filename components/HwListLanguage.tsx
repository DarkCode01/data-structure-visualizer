import styled from "styled-components";

const HwListLanguage = styled.div`
  margin-top: 40px;
  margin-bottom: 20px;

  display: grid;
  grid-template-columns: repeat(5, 1fr);
  grid-gap: 10px;
`;

export default HwListLanguage;
